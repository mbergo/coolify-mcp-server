# MCP Tools

> Auto-generated by `scripts/gen-docs.mjs`. Do not hand-edit.

**Total tools:** 116

**Generated:** 2026-04-19T20:57:52.006Z

## Index

- [Applications](#applications) — 20 tools
- [Cloud tokens](#cloud-tokens) — 6 tools
- [Composite / misc](#composite-misc) — 5 tools
- [Databases](#databases) — 21 tools
- [Deployments](#deployments) — 4 tools
- [GitHub Apps](#github-apps) — 7 tools
- [Hetzner](#hetzner) — 5 tools
- [Private keys](#private-keys) — 5 tools
- [Projects](#projects) — 10 tools
- [Search & resources](#search-resources) — 2 tools
- [Servers](#servers) — 8 tools
- [Services](#services) — 13 tools
- [System](#system) — 5 tools
- [Teams](#teams) — 5 tools

## Applications

### `bulk_update_app_envs`

**Title:** Bulk update application env vars

**Description:** Replace / upsert several env vars in one PATCH.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "envs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string"
          },
          "value": {
            "type": "string"
          },
          "is_preview": {
            "type": "boolean"
          },
          "is_build_time": {
            "type": "boolean"
          },
          "is_literal": {
            "type": "boolean"
          },
          "is_multiline": {
            "type": "boolean"
          },
          "is_shown_once": {
            "type": "boolean"
          }
        },
        "required": [
          "key"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "uuid",
    "envs"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_app_env`

**Title:** Create application env var

**Description:** Add a new environment variable to an application.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "env": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "is_preview": {
          "type": "boolean"
        },
        "is_build_time": {
          "type": "boolean"
        },
        "is_literal": {
          "type": "boolean"
        },
        "is_multiline": {
          "type": "boolean"
        },
        "is_shown_once": {
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "uuid",
    "env"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_compose_app`

**Title:** Create application (from docker-compose)

**Description:** Creates an app from inline docker-compose YAML contents.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "server_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Descriptive base name (auto-suffixed with -11d)"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    },
    "docker_compose_raw": {
      "type": "string",
      "description": "docker-compose YAML"
    },
    "domains": {
      "type": "string"
    }
  },
  "required": [
    "project_uuid",
    "server_uuid",
    "docker_compose_raw"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_deploy_key_app`

**Title:** Create application (private via deploy key)

**Description:** Creates an app using a stored deploy key for repo auth.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "server_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Descriptive base name (auto-suffixed with -11d)"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    },
    "private_key_uuid": {
      "type": "string"
    },
    "git_repository": {
      "type": "string"
    },
    "git_branch": {
      "type": "string",
      "default": "main"
    },
    "build_pack": {
      "type": "string",
      "enum": [
        "nixpacks",
        "static",
        "dockerfile",
        "dockercompose",
        "dockerimage"
      ],
      "default": "nixpacks"
    },
    "ports_exposes": {
      "type": "string"
    }
  },
  "required": [
    "project_uuid",
    "server_uuid",
    "private_key_uuid",
    "git_repository"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_dockerfile_app`

**Title:** Create application (from Dockerfile)

**Description:** Creates an app from inline Dockerfile contents.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "server_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Descriptive base name (auto-suffixed with -11d)"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    },
    "dockerfile": {
      "type": "string",
      "description": "Dockerfile contents"
    },
    "ports_exposes": {
      "type": "string"
    },
    "domains": {
      "type": "string"
    }
  },
  "required": [
    "project_uuid",
    "server_uuid",
    "dockerfile"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_dockerimage_app`

**Title:** Create application (from Docker image)

**Description:** Creates an app from a Docker registry image.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "server_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Descriptive base name (auto-suffixed with -11d)"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    },
    "docker_registry_image_name": {
      "type": "string"
    },
    "docker_registry_image_tag": {
      "type": "string",
      "default": "latest"
    },
    "ports_exposes": {
      "type": "string"
    },
    "domains": {
      "type": "string"
    }
  },
  "required": [
    "project_uuid",
    "server_uuid",
    "docker_registry_image_name"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_public_app`

**Title:** Create application (public git)

**Description:** Creates an app from a public repository; renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "server_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Descriptive base name (auto-suffixed with -11d)"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    },
    "git_repository": {
      "type": "string"
    },
    "git_branch": {
      "type": "string",
      "default": "main"
    },
    "build_pack": {
      "type": "string",
      "enum": [
        "nixpacks",
        "static",
        "dockerfile",
        "dockercompose",
        "dockerimage"
      ],
      "default": "nixpacks"
    },
    "ports_exposes": {
      "type": "string"
    },
    "domains": {
      "type": "string"
    }
  },
  "required": [
    "project_uuid",
    "server_uuid",
    "git_repository"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_app_env`

**Title:** Delete application env var

**Description:** Remove a single env var by UUID. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "env_uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "env_uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_application`

**Title:** Delete application

**Description:** Permanently delete an application. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_app_logs`

**Title:** Get application logs

**Description:** Fetch application log buffer.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "lines": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 10000
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_application`

**Title:** Get application

**Description:** Fetch a single application by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_app_deployments`

**Title:** List application deployments

**Description:** Recent deployments for a specific application.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_app_envs`

**Title:** List application env vars

**Description:** Environment variables for an application (values redacted by default).

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_applications`

**Title:** List applications

**Description:** List applications. Compact by default. Filter by server_uuid or project_uuid.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    },
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `restart_application`

**Title:** Restart application

**Description:** restart an application by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `restart_project_apps`

**Title:** Restart all apps in a project

**Description:** Restart every application inside the given project UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "project_uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `start_application`

**Title:** Start application

**Description:** start an application by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `stop_application`

**Title:** Stop application

**Description:** stop an application by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_app_env`

**Title:** Update application env var

**Description:** Update a single application env var.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "env": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "is_preview": {
          "type": "boolean"
        },
        "is_build_time": {
          "type": "boolean"
        },
        "is_literal": {
          "type": "boolean"
        },
        "is_multiline": {
          "type": "boolean"
        },
        "is_shown_once": {
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "uuid",
    "env"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_application`

**Title:** Update application

**Description:** PATCH application fields. Pass a `patch` object with any Coolify fields.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Cloud tokens

### `create_cloud_token`

**Title:** Create cloud token

**Description:** Register a new cloud-provider token.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "payload"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_cloud_token`

**Title:** Delete cloud token

**Description:** Permanently remove a cloud token. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_cloud_token`

**Title:** Get cloud token

**Description:** —

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_cloud_tokens`

**Title:** List cloud tokens

**Description:** List configured cloud-provider tokens (values redacted).

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_cloud_token`

**Title:** Update cloud token

**Description:** —

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `validate_cloud_token`

**Title:** Validate cloud token

**Description:** Ping the cloud provider with the stored token.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Composite / misc

### `bulk_env_update`

**Title:** Bulk env var upsert across apps

**Description:** Upsert a single env var (key/value) across multiple applications. Each app receives one createEnv call.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "app_uuids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "env": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "is_preview": {
          "type": "boolean"
        },
        "is_build_time": {
          "type": "boolean"
        },
        "is_literal": {
          "type": "boolean"
        }
      },
      "required": [
        "key",
        "value"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "app_uuids",
    "env"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `emergency_stop_all`

**Title:** EMERGENCY — stop all running applications

**Description:** Stops every running application across the entire Coolify instance. Strictly destructive — requires confirm:true. Returns a per-app success/failure report.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    },
    "server_uuid": {
      "type": "string",
      "description": "Optional: restrict to a single server"
    }
  },
  "required": [
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `preview_elevend_name`

**Title:** Preview -11d name

**Description:** Non-destructive: show what <base> would become under current suffix + collision policy (doesn't query existing names).

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "base": {
      "type": "string"
    }
  },
  "required": [
    "base"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `rename_resource`

**Title:** Rename resource (apply -11d convention)

**Description:** Apply the -11d naming convention to an existing resource. Strips supabase-<sha>, sanitises unsafe chars, resolves collisions via the configured policy, and issues PATCH.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "application",
        "database",
        "service",
        "server",
        "project"
      ]
    },
    "uuid": {
      "type": "string"
    },
    "desired_name": {
      "type": "string",
      "description": "Descriptive base name (without -11d)"
    }
  },
  "required": [
    "kind",
    "uuid",
    "desired_name"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `status_overview`

**Title:** Status overview

**Description:** Dashboard snapshot: server count + reachability, app counts by status, recent deployments, database + service counts.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "limit_deployments": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 50,
      "default": 10
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Databases

### `create_clickhouse_database`

**Title:** Create clickhouse database

**Description:** Creates a clickhouse database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_db_backup`

**Title:** Create DB backup

**Description:** Trigger/configure a new backup.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "payload": {
      "type": "object",
      "additionalProperties": {},
      "default": {}
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_dragonfly_database`

**Title:** Create dragonfly database

**Description:** Creates a dragonfly database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_keydb_database`

**Title:** Create keydb database

**Description:** Creates a keydb database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_mariadb_database`

**Title:** Create mariadb database

**Description:** Creates a mariadb database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_mongodb_database`

**Title:** Create mongodb database

**Description:** Creates a mongodb database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_mysql_database`

**Title:** Create mysql database

**Description:** Creates a mysql database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_postgres_database`

**Title:** Create postgres database

**Description:** Creates a postgres database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_redis_database`

**Title:** Create redis database

**Description:** Creates a redis database and renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean"
    },
    "public_port": {
      "type": "integer"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_backup_exec`

**Title:** Delete backup execution

**Description:** Remove a backup execution record. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "exec_uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "exec_uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_database`

**Title:** Delete database

**Description:** Permanently delete a database. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_db_backup`

**Title:** Delete DB backup

**Description:** Remove a backup. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "backup_uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "backup_uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_database`

**Title:** Get database

**Description:** Fetch a single database by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_backup_execs`

**Title:** List backup executions

**Description:** List execution history for a database's backups.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_databases`

**Title:** List databases

**Description:** List all databases.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_db_backups`

**Title:** List DB backups

**Description:** List backups for a database.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `restart_database`

**Title:** Restart database

**Description:** restart a database by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `start_database`

**Title:** Start database

**Description:** start a database by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `stop_database`

**Title:** Stop database

**Description:** stop a database by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_database`

**Title:** Update database

**Description:** PATCH database fields.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_db_backup`

**Title:** Update DB backup config

**Description:** PATCH backup settings for a database.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Deployments

### `cancel_deployment`

**Title:** Cancel deployment

**Description:** Cancel an in-flight deployment. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_deployment`

**Title:** Get deployment

**Description:** Fetch a single deployment by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_deployments`

**Title:** List deployments

**Description:** List recent deployments.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    },
    "limit": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 200
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `trigger_deploy`

**Title:** Trigger deployment

**Description:** Trigger a deploy. Pass `uuid` (application) or `tag` (image tag). Use `force:true` to bypass build cache.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "tag": {
      "type": "string"
    },
    "force": {
      "type": "boolean"
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## GitHub Apps

### `create_github_app`

**Title:** Create application (private via GitHub App)

**Description:** Creates an app using a GitHub App installation for repo auth.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "server_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Descriptive base name (auto-suffixed with -11d)"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    },
    "github_app_uuid": {
      "type": "string"
    },
    "git_repository": {
      "type": "string"
    },
    "git_branch": {
      "type": "string",
      "default": "main"
    },
    "build_pack": {
      "type": "string",
      "enum": [
        "nixpacks",
        "static",
        "dockerfile",
        "dockercompose",
        "dockerimage"
      ],
      "default": "nixpacks"
    },
    "ports_exposes": {
      "type": "string"
    }
  },
  "required": [
    "project_uuid",
    "server_uuid",
    "github_app_uuid",
    "git_repository"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_github_app`

**Title:** Delete GitHub App

**Description:** Remove a GitHub App integration. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_gh_branches`

**Title:** List GitHub branches

**Description:** List branches for a specific repository under a GitHub App.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "owner": {
      "type": "string"
    },
    "repo": {
      "type": "string"
    }
  },
  "required": [
    "uuid",
    "owner",
    "repo"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_gh_repos`

**Title:** List GitHub repos

**Description:** List repositories accessible to a GitHub App installation.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_github_apps`

**Title:** List GitHub Apps

**Description:** List configured GitHub App integrations.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `register_github_app`

**Title:** Register GitHub App

**Description:** Register a new GitHub App installation (the App itself, not a Coolify application using the App).

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "payload"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_github_app`

**Title:** Update GitHub App

**Description:** —

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Hetzner

### `create_hetzner_server`

**Title:** Create Hetzner server

**Description:** Provision a new Hetzner cloud server and register it with Coolify.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "payload"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `hetzner_images`

**Title:** Hetzner images

**Description:** List available Hetzner OS images.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `hetzner_locations`

**Title:** Hetzner locations

**Description:** List available Hetzner datacenter locations.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `hetzner_server_types`

**Title:** Hetzner server types

**Description:** List available Hetzner server types / plans.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `hetzner_ssh_keys`

**Title:** Hetzner SSH keys

**Description:** List SSH keys configured in the Hetzner account.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Private keys

### `create_key`

**Title:** Create private key

**Description:** Register a new SSH private key. Payload fields per Coolify API.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "payload"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_key`

**Title:** Delete private key

**Description:** Remove a stored private key. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_key`

**Title:** Get private key

**Description:** Fetch a single private key by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_keys`

**Title:** List private keys

**Description:** List all configured SSH private keys (sensitive material redacted).

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_key`

**Title:** Update private key

**Description:** PATCH private-key fields.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Projects

### `create_project`

**Title:** Create project

**Description:** Create a project; renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_project_env`

**Title:** Create project environment

**Description:** Add a new environment to a project.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string",
      "description": "Project UUID"
    },
    "name": {
      "type": "string",
      "description": "Environment name"
    }
  },
  "required": [
    "uuid",
    "name"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_project`

**Title:** Delete project

**Description:** Permanently delete a project. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_project_env`

**Title:** Delete project environment

**Description:** Remove an environment from a project. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string",
      "description": "Project UUID"
    },
    "env_name": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "env_name",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_project`

**Title:** Get project

**Description:** Fetch a single project by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_project_env`

**Title:** Get project environment

**Description:** Fetch a single environment under a project.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string",
      "description": "Project UUID"
    },
    "env_name": {
      "type": "string"
    }
  },
  "required": [
    "uuid",
    "env_name"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_project_envs`

**Title:** List project environments

**Description:** Environments defined under a project.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_projects`

**Title:** List projects

**Description:** List all projects.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `redeploy_project`

**Title:** Redeploy all apps in a project

**Description:** Force-rebuild + redeploy every application in a project via /deploy?uuid=<app>&force=true. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "project_uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "project_uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_project`

**Title:** Update project

**Description:** PATCH project fields.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Search & resources

### `list_resources`

**Title:** List all resources

**Description:** GET /resources — generic cross-type resource list.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `search_resources`

**Title:** Search resources

**Description:** Smart search across applications, databases, services, servers, and projects. Matches UUID, exact name, domain/fqdn, server IP, or fuzzy name.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "application",
          "database",
          "service",
          "server",
          "project"
        ]
      }
    },
    "limit": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 50,
      "default": 20
    },
    "fuzzyThreshold": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0.4
    }
  },
  "required": [
    "query"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Servers

### `create_server`

**Title:** Create server

**Description:** Register a new server; renames to <name>-11d post-create.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "ip": {
      "type": "string"
    },
    "port": {
      "type": "integer"
    },
    "user": {
      "type": "string"
    },
    "private_key_uuid": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "is_build_server": {
      "type": "boolean"
    },
    "instant_validate": {
      "type": "boolean"
    }
  },
  "required": [
    "name",
    "ip",
    "private_key_uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_server`

**Title:** Delete server

**Description:** Permanently delete a server. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_server`

**Title:** Get server

**Description:** Fetch a single server by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_servers`

**Title:** List servers

**Description:** List all Coolify servers.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `server_domains`

**Title:** Server domains

**Description:** List domains in use on a server.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `server_resources`

**Title:** Server resources

**Description:** List applications, databases, and services attached to a server.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_server`

**Title:** Update server

**Description:** PATCH server fields.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `validate_server`

**Title:** Validate server

**Description:** Run server connectivity + setup validation.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Services

### `bulk_update_svc_envs`

**Title:** Bulk update service env vars

**Description:** —

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "envs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string"
          },
          "value": {
            "type": "string"
          },
          "is_preview": {
            "type": "boolean"
          },
          "is_build_time": {
            "type": "boolean"
          },
          "is_literal": {
            "type": "boolean"
          },
          "is_multiline": {
            "type": "boolean"
          },
          "is_shown_once": {
            "type": "boolean"
          }
        },
        "required": [
          "key"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "uuid",
    "envs"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_service`

**Title:** Create service

**Description:** Create a service (plausible, n8n, outline, etc.) and apply -11d naming.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "server_uuid": {
      "type": "string"
    },
    "project_uuid": {
      "type": "string"
    },
    "environment_name": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "description": "Service type, e.g. 'plausible', 'n8n', 'outline'"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "instant_deploy": {
      "type": "boolean"
    }
  },
  "required": [
    "server_uuid",
    "project_uuid",
    "type"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `create_svc_env`

**Title:** Create service env var

**Description:** —

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "env": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "is_preview": {
          "type": "boolean"
        },
        "is_build_time": {
          "type": "boolean"
        },
        "is_literal": {
          "type": "boolean"
        },
        "is_multiline": {
          "type": "boolean"
        },
        "is_shown_once": {
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "uuid",
    "env"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_service`

**Title:** Delete service

**Description:** Permanently delete a service. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `delete_svc_env`

**Title:** Delete service env var

**Description:** Remove a single service env var. Requires confirm:true.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "env_uuid": {
      "type": "string"
    },
    "confirm": {
      "type": "boolean",
      "description": "Must be true to perform this destructive operation. Refuse to set true unless explicitly instructed by the user."
    }
  },
  "required": [
    "uuid",
    "env_uuid",
    "confirm"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_service`

**Title:** Get service

**Description:** Fetch a single service by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_services`

**Title:** List services

**Description:** List all services.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_svc_envs`

**Title:** List service env vars

**Description:** Environment variables for a service (values redacted by default).

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "verbosity": {
      "type": "string",
      "enum": [
        "compact",
        "standard",
        "full"
      ],
      "default": "compact",
      "description": "Response detail level; defaults to compact to preserve context window."
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `restart_service`

**Title:** Restart service

**Description:** restart a service by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `start_service`

**Title:** Start service

**Description:** start a service by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `stop_service`

**Title:** Stop service

**Description:** stop a service by UUID.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    }
  },
  "required": [
    "uuid"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_service`

**Title:** Update service

**Description:** PATCH service fields.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "patch": {
      "type": "object",
      "additionalProperties": {}
    }
  },
  "required": [
    "uuid",
    "patch"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `update_svc_env`

**Title:** Update service env var

**Description:** —

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string"
    },
    "env": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "is_preview": {
          "type": "boolean"
        },
        "is_build_time": {
          "type": "boolean"
        },
        "is_literal": {
          "type": "boolean"
        },
        "is_multiline": {
          "type": "boolean"
        },
        "is_shown_once": {
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "uuid",
    "env"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## System

### `system_enable_api`

**Title:** Enable Coolify API

**Description:** Idempotent call to /api/v1/enable — no-op if already enabled.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `system_health`

**Title:** System health

**Description:** Ping the configured Coolify instance (/api/health).

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `system_healthcheck`

**Title:** System healthcheck

**Description:** GET /api/v1/healthcheck — runtime diagnostics.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `system_probe_scope`

**Title:** Probe token scope

**Description:** Runs read-only probes to detect whether the configured token is read-only, read:sensitive, or *.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `system_version`

**Title:** Coolify version

**Description:** Return the Coolify instance version.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

## Teams

### `current_team`

**Title:** Current team

**Description:** The team associated with the configured API token.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `current_team_members`

**Title:** Current team members

**Description:** Members of the token's team.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `get_team`

**Title:** Get team

**Description:** Fetch a team by id.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": [
        "string",
        "number"
      ]
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `list_teams`

**Title:** List teams

**Description:** List all teams.

**Input schema:**

```json
{
  "type": "object",
  "properties": {},
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

### `team_members`

**Title:** Team members

**Description:** List members of a team.

**Input schema:**

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": [
        "string",
        "number"
      ]
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
```

